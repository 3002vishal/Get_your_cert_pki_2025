<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Certificate of Participation - <%= conference.name %></title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Times+New+Roman:wght@400;700&display=swap');
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Times New Roman', serif;
            background: #f5f5f5;
            padding: 20px;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }
        
        .certificate {
            width: 210mm;
            height: 297mm;
            background: white;
            border: 4px solid #1e3a8a;
            padding: 30mm;
            position: relative;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
        }
        
        .header-section {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 20px;
        }
        
        .left-logo {
            width: 80px;
            height: 80px;
        }
        
        .center-content {
            flex: 1;
            text-align: center;
            margin: 0 20px;
        }
        
        .right-logo {
            width: 80px;
            height: 80px;
        }
        
        .hindi-text {
            font-size: 16px;
            font-weight: bold;
            color: #333;
            margin-bottom: 5px;
        }
        
        .org-name {
            font-size: 18px;
            font-weight: bold;
            color: #1e3a8a;
            margin-bottom: 8px;
            letter-spacing: 1px;
        }
        
        .govt-text {
            font-size: 12px;
            color: #666;
            font-style: italic;
            line-height: 1.3;
        }
        
        .signatures-section {
            display: flex;
            justify-content: space-between;
            margin: 25px 0;
        }
        
        .signature-box {
            text-align: center;
            width: 45%;
        }
        
        .signature-name {
            font-size: 14px;
            font-weight: bold;
            color: #333;
            margin-bottom: 3px;
        }
        
        .signature-title {
            font-size: 11px;
            color: #666;
            line-height: 1.2;
        }
        
        .certificate-title {
            text-align: center;
            font-size: 28px;
            font-weight: bold;
            color: #dc2626;
            margin: 30px 0;
            text-decoration: underline;
            text-decoration-color: #dc2626;
        }
        
        .certificate-content {
            text-align: justify;
            font-size: 16px;
            line-height: 1.8;
            color: #333;
            margin: 25px 0;
        }
        
        .participant-name {
            font-weight: bold;
            font-size: 18px;
            color: #1e3a8a;
        }
        
        .highlight {
            font-weight: bold;
        }
        
        .conference-name {
            font-weight: bold;
            font-style: italic;
        }
        
        .bottom-logos {
            position: absolute;
            bottom: 30mm;
            left: 50%;
            transform: translateX(-50%);
            display: flex;
            gap: 30px;
            align-items: center;
        }
        
        .bottom-logo {
            width: 70px;
            height: 70px;
        }
        
        /* Print styles */
        @media print {
            body {
                background: white;
                padding: 0;
            }
            
            .certificate {
                box-shadow: none;
                border: 2px solid #1e3a8a;
            }
        }
    </style>
</head>
<body>
    <div class="certificate">
        <!-- Header Section -->
        <div class="header-section">
            <div class="left-logo">
                <img src="<%= logos.ias %>" alt="IEEE Industry Applications Society" style="width: 100%; height: 100%; object-fit: contain;">
            </div>
            
            <div class="center-content">
                <div class="hindi-text"><%= organization.nameHindi %></div>
                <div class="org-name"><%= organization.nameEnglish %></div>
                <div class="govt-text"><%= organization.descriptionHindi %></div>
                <div class="govt-text"><%= organization.descriptionEnglish %></div>
            </div>
            
            <div class="right-logo">
                <img src="<%= logos.digitalTrust %>" alt="National Centre for Digital Trust" style="width: 100%; height: 100%; object-fit: contain;">
            </div>
        </div>
        
        <!-- Signatures Section -->
        <div class="signatures-section">
            <div class="signature-box">
                <div class="signature-name"><%= signatures.coChair.name %></div>
                <div class="signature-title"><%= signatures.coChair.title %></div>
            </div>
            
            <div class="signature-box">
                <div class="signature-name"><%= signatures.organizer.name %></div>
                <div class="signature-title"><%= signatures.organizer.title %></div>
            </div>
        </div>
        
        <!-- Certificate Title -->
        <div class="certificate-title"><%= certificate.title %></div>
        
        <!-- Certificate Content -->
        <div class="certificate-content">
            <span class="participant-name"><%= participant.title %> <%= participant.name %></span> 
            of <%= participant.organization %>, attended the 
            <span class="conference-name"><%= conference.edition %><sup><%= conference.editionSuffix %></sup> International Conference on "<%= conference.fullName %>"</span> 
            on <span class="highlight"><%= event.attendanceDate %><sup><%= event.attendanceDateSuffix %></sup> of <%= event.attendanceMonth %></span>, 
            organized by <span class="highlight"><%= event.organizer %></span> 
            at the <span class="highlight"><%= event.venue %></span> 
            during <span class="highlight"><%= event.duration %></span>.
        </div>
        
        <!-- Bottom Logos -->
        <div class="bottom-logos">
            <div class="bottom-logo">
                <img src="<%= logos.cca %>" alt="Controller of Certifying Authorities" style="width: 100%; height: 100%; object-fit: contain;">
            </div>
            
            <div class="bottom-logo">
                <img src="<%= logos.ieeeCs %>" alt="IEEE Computer Society Bangalore Chapter" style="width: 100%; height: 100%; object-fit: contain;">
            </div>
        </div>
    </div>
</body>
</html>